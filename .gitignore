# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
desktop.ini
$RECYCLE.BIN/

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
*.sublime-workspace
*.code-workspace

# Local environment and secrets
.env
.env.*
!.env.example
*.local
.envrc
.direnv/

# Claude Code local state (settings.local.json is per-machine, not shared)
.claude/settings.local.json

# Hook scratch. Per-session and per-machine: the HEAD a session started at, the
# last gate result, the Stop-hook block counter, and the PreCompact handoff
# note. .claude/activity.jsonl is deliberately NOT here -- ADR-0003 commits it.
.claude/.hook-state/
.claude/in-flight.md

# Logs and scratch
*.log
logs/
tmp/
scratch/
